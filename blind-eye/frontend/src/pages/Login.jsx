// Login page. Calls POST /auth/login and stores the returned JWT.
export default function Login() {
  return (
    <div className="flex items-center justify-center h-screen">
      <p className="text-gray-500">Login page — build here</p>
    </div>
  );
}
